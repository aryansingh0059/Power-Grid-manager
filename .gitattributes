# Normalize line endings to LF in the repo; checkout as-is on all platforms.
* text=auto eol=lf

# Force these to always be LF (important for Docker/bash scripts)
*.sh text eol=lf
Dockerfile text eol=lf
nginx.conf text eol=lf
